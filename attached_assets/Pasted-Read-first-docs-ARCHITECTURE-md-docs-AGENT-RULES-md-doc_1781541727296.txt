Read first:

* docs/ARCHITECTURE.md
* docs/AGENT_RULES.md
* docs/CHANGELOG.md

STOP FEATURE DEVELOPMENT.

We are entering a stabilization sprint.

User feedback:

* All topics currently fail to generate.
* Multiple versions of the application appear to exist.
* User cannot determine which application is the active version.
* Core functionality is broken.
* Product trust is at risk.

Goal:

Restore reliability before adding any new features.

========================================
TASK A — ROOT CAUSE INVESTIGATION

Find the exact reason why ALL topic briefings fail.

Do not guess.

Instrument the full request chain:

UI
↓
API Route
↓
News Collection
↓
Prompt Builder
↓
AI Provider
↓
Response Parser

Log each step.

Output:

* where failure occurs
* error message
* affected provider
* affected topic
* reproduction steps

========================================
TASK B — SINGLE ACTIVE APPLICATION

Investigate why two applications appear.

Requirements:

* identify duplicate routes
* identify duplicate frontends
* identify duplicate pages
* identify old preview builds

Create:

SYSTEM_STATUS.md

Document:

* active entry point
* active homepage
* active routes
* deprecated routes

Only one application should exist.

========================================
TASK C — HEALTH CHECK ENDPOINT

Create:

GET /api/health

Response:

{
status,
rssFeedsWorking,
aiProviderWorking,
storageWorking,
timestamp
}

Add visible status badge:

System Healthy
System Degraded
System Offline

========================================
TASK D — FAILSAFE MODE

If AI generation fails:

Show collected articles anyway.

User must never see a blank newsroom.

Fallback:

Headline
Source
Link
Date

Always provide something useful.

========================================
TASK E — PROVIDER DIAGNOSTICS

Create diagnostics page.

Display:

AI Provider
Model
Token estimate
Request duration
Failure reason

Developer mode only.

========================================
TASK F — CODE CLEANUP

Remove:

dead code
unused pages
duplicate components
deprecated routes

Update architecture documentation.

========================================
SUCCESS CRITERIA

Before sprint completion:

* every topic loads successfully
    OR
* fallback article mode activates

No blank page.

No duplicate applications.

Health endpoint operational.

Provide final report including:

Root cause
Files changed
Files removed
Known issues
Remaining risks